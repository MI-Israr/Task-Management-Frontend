import React from "react";

const TaskCard = ({
  title,
  description,
  status,
  priority,
  dueDate,
  assignedTo,
  todoChecklist,
  attachments,
  createdAt,
  updatedAt,
  onClick,
}) => {
  return <div>TaskCard</div>;
};

export default TaskCard;
